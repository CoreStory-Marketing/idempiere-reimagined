package com.corestory.idempiere.notifications.model;

/**
 * Channel — mirrors {@code notification_log.channel} CHECK constraint.
 */
public enum NotificationChannel {
    EMAIL,
    WAREHOUSE,
    ACCOUNTING,
    SMS,
    IN_APP
}
